// Use a relative base so the React dev server (CRA) can proxy to the backend
// and production can use same-origin requests without manual edits.
const API_BASE_URL = '/api';

export default API_BASE_URL;
